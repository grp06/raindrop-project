# Frontend

Fitness Data Explorer UI.

## Setup

```bash
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:8000`, so start the backend there.

## Build

```bash
npm run build
npm run preview
```
